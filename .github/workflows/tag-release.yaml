name: Release tagged image

on:
  release:
    types: [ released ]

jobs:
  release:
    name: Push image to latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Pull prerelease and pull as latest
        run: |
          docker build \
            --build-arg PDNS_VERSION="$GITHUB_REF_NAME" \
            -t ghcr.io/${{ github.repository }}:"$GITHUB_REF_NAME" \
            -t ghcr.io/${{ github.repository }}:latest
            .

          docker push ghcr.io/${{ github.repository }}:"$GITHUB_REF_NAME"
          docker push ghcr.io/${{ github.repository }}:latest
